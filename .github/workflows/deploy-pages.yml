name: Deploy Fuori Forno to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Configure public URL and base path
        shell: bash
        env:
          CONFIGURED_SITE_URL: ${{ vars.SITE_URL }}
          CONFIGURED_BASE_PATH: ${{ vars.PAGES_BASE_PATH }}
          DETECTED_SITE_URL: ${{ steps.pages.outputs.base_url }}
          DETECTED_BASE_PATH: ${{ steps.pages.outputs.base_path }}
        run: |
          if [[ -n "$CONFIGURED_SITE_URL" ]]; then
            SITE_URL="${CONFIGURED_SITE_URL%/}"
          else
            SITE_URL="${DETECTED_SITE_URL%/}"
          fi

          if [[ -n "$CONFIGURED_BASE_PATH" ]]; then
            BASE_PATH="${CONFIGURED_BASE_PATH%/}"
          else
            BASE_PATH="${DETECTED_BASE_PATH%/}"
          fi

          echo "NEXT_PUBLIC_BASE_PATH=$BASE_PATH" >> "$GITHUB_ENV"
          echo "NEXT_PUBLIC_SITE_URL=$SITE_URL" >> "$GITHUB_ENV"

      - name: Build static site
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
